#include <stdio.h>
#include <limits.h>
#include <stdlib.h>
#include <math.h>

int main(void) {
    int t;
    int n; 
    scanf("%d", &t);
    while (t--){
        int i = 0;
        scanf("%d", &n);
        int arr[n];
        scanf("%d", &arr[i++]);
    }
    return 0;
}